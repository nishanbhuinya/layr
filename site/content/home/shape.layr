Page(
  .name(Profile)
  .route('/')
  Scaffold(.body(Row(
    .config(w: fill, gap: 16, padding: all(24), yAlign: mid)
    Container(.config(size: 56, color: accent, cornerRadius: 999))
    Column(
      .config(w: fill, gap: 4)
      Text(.config(size: 18, weight: semibold) .obj('Ada Lovelace'))
      Text(.config(color: muted) .obj('Analyst, Analytical Engine'))
    )
  )))
)

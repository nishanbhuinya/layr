/** Runs once per page load, before the app: fonts, the site stylesheet, and consent. */
import "@fontsource-variable/geist";
import "@fontsource-variable/geist-mono";
import "../site.css";
import "./consent.ts";

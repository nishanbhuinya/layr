# {{name}}

A [LAYR](https://layr.dynshift.com) app.

```sh
npm install
npx layr dev       # start the dev server
npx layr analyze   # check the project
npx layr build     # prerendered static site in dist/
```
